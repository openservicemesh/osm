kind: Service
apiVersion: v1
metadata:
  name: zipkin
  labels:
    app: zipkin
spec:
  selector:
    app: zipkin
  ports:
  - protocol: TCP
    port: {{.Values.zipkin.port}}
    targetPort: {{.Values.zipkin.port}}
  type: ClusterIP
