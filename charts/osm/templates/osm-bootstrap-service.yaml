apiVersion: v1
kind: Service
metadata:
  name: osm-bootstrap
  namespace: {{ include "osm.namespace" . }}
  labels:
    {{- include "osm.labels" . | nindent 4 }}
    app: osm-bootstrap
spec:
  ports:
    - name: crdconversion
      port: 9443
  selector:
    app: osm-bootstrap
